import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const lock = () => {
  return (
    <View>
      <Text>lock</Text>
    </View>
  );
};

export default lock;

const styles = StyleSheet.create({});
